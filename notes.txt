
Remaining todos/ideas:
- Show quotes in completion candidates
- Filter suggestions that need backticks for unquoted input etc.
- Use types for `$` environment and list access
- Refine types, e.g. for quoted
- Include names of loaded namespaces/installed packages in global completion candidates
- Implement function argument names
    - Installed packages for `library`

